[project]
name = "ri-cloud-api"
authors = [{ name = "Jørgen Herje", email = "jorgen.herje@gmail.com" }]
requires-python = ">=3.11"
version = "0.0.0"
description = "Local-hosted backend service bridging Sumo data into ResInsight over a FastAPI HTTP API."
readme = "README.md"
dependencies = [
    "fastapi>=0.115.0,<0.116.0",
    "uvicorn[standard]>=0.32.0,<0.33.0",
    "pydantic>=2.9.0,<3.0.0",
    "fmu-sumo>=2.15.0,<3.0.0",
    "sumo-wrapper-python>=1.7.0,<2.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0,<0.17.0",
    "mypy>=1.18.2,<2.0.0",
    "pytest>=9.0.3,<10.0.0",
]

[tool.hatch.build.targets.wheel]
packages = [
    "ri_cloud_api",
    "ri_cloud_api/libs/services/src/ri_cloud_services",
    "ri_cloud_api/libs/core_utils/src/ri_cloud_core_utils",
]

[tool.ruff]
line-length = 120


[tool.mypy]
python_version = "3.13"
warn_unused_configs = true
disallow_untyped_defs = true
explicit_package_bases = true
mypy_path = [
    "ri_cloud_api/libs/services/src",
    "ri_cloud_api/libs/core_utils/src",
]

[[tool.mypy.overrides]]
module = [
    "fmu.sumo.*",
    "sumo.wrapper.*",
]
follow_untyped_imports = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
